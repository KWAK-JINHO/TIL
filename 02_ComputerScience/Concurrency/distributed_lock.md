# Distributed Lock

## Redis 분산 락

### 정의

분산 시스템 환경에서 여러 서버가 동시에 공유 자원(캐시, 파일, DB 등)에 접근할 때 사용하는 락. Redis의 원자적 연산을 활용해 구현한다.

### 구현

- SET if Not eXists 명령어로 키가 없을 때만 값을 설정
- 락을 획득한 서버만 `1`을 반환받고, 나머지는 `0`을 반환받음
- 작업 완료 후 DEL 명령어로 키를 삭제해 락 해제
- EXPIRE로 타임아웃 설정해 데드락 방지