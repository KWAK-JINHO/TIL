# Generics

제네릭스는 클래스나 메서드에서 사용할 내부 데이터 타입을 컴파일 시점에 지정하는 기술. 클래스 작성 시 Object대신 타입변수 <T> 사용하여 선언.

## 사용

- 객체 생성 시 참조변수와 생성자에 대입된 타입이 일치해야한다.
    - `Box<Apple> appleBox = new Box<Apple>`

- 타입이 상속 관계에 있어도 타입변수 <T>는 클래스 간의 상속관계를 무시
    - `List<Object> list = new ArrayList<String>();` 불가능
    - `List<String> list = new ArrayList<String>();` 가능. 클래스 자체는 다형성 적용

- `extends`를 사용해 타입 제한. `&`사용해 상속과 인터페이스 구현 동시 가능

## 제약사항

- static 멤버는 타입변수 <T>사용 불가.
- 제네릭 타입의 배열생성 불가

## 와일드 카드

제네릭 타입이 다른것 만으로 오버로딩이 성립하지 않기 때문에 와일드카드가 필요하다.

- `<? extends T>`: T와 자손들만 가능
- `<? super T>`: T와 조상들만 가능
- `<?>`: 제한 없이 모두 가능